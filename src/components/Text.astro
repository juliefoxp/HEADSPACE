---
const { teksttype = "brødtekst", stylingclasses = "" } = Astro.props;

// Tailwind styling for teksttyper
const sizeClasses = {
  // Brødtekst (body)
  "brødtekst": "text-[18px] lg:text-[18px] font-roboto",
};
---

<p class:list={[sizeClasses[teksttype], stylingclasses]}>
  <slot />
</p>
